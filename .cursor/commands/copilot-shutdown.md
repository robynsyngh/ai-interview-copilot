# Copilot: shutdown

Stop everything started by bootup: the Chrome extension watcher **and** all
Docker containers (postgres, pgadmin, api, web).

Run from the repo root:

```bash
./copilot shutdown
```
