# Copilot: health

Curl the API health endpoint to confirm the backend is reachable.

Run from the repo root:

```bash
./copilot health
```

Expected: `{"status":"ok","version":"..."}` from http://localhost:8000/health
