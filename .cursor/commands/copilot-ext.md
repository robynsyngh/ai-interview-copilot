# Copilot: ext

Run the Chrome extension dev build / watcher (`pnpm dev:ext`). Use this to pick
up side-panel UI changes, then reload the unpacked extension in Chrome.

Run from the repo root:

```bash
./copilot ext
```

This is a watcher and runs continuously, so start it as a long-running/background
process.
